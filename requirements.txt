# --- Core ---
numpy==1.26.4            # stable, compatible with PyTorch + Ultralytics
matplotlib==3.8.4
Pillow==10.3.0
tqdm==4.66.4
requests==2.32.3

# --- CV + OCR ---
opencv-python==4.7.0.72      # stable with contrib support
opencv-contrib-python==4.7.0.72
easyocr==1.7.1               # works with numpy<=1.26
pytesseract==0.3.13          # needs local Tesseract installed

# --- Detection ---
ultralytics==8.2.77          # stable YOLOv8 (works with numpy<=1.26)

# --- Speech (TTS) ---
pyttsx3==2.90

# --- Speech (voice commands, offline) ---
vosk==0.3.45
sounddevice==0.4.7
python-bidi==0.6.6
PyYAML==6.0.1

# --- Geospatial / Routing ---
geopy==2.4.1
polyline==2.0.2
googlemaps==4.10.0           # optional (if using Google Directions API)

# --- Dev (optional) ---
jupyter==1.0.0
